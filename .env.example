# Copy to .env (`make .env` does this) and fill in the blanks.
# Nothing here is required for `make up` — only the pipeline tickets need real keys.

# --- Database ---------------------------------------------------------------
POSTGRES_USER=underwrite
POSTGRES_PASSWORD=underwrite
POSTGRES_DB=underwrite
# Host port only — deliberately not 5432 so this never collides with another
# project's Postgres. Inside the compose network the port is always 5432.
POSTGRES_PORT=55432
DATABASE_URL=postgresql+asyncpg://underwrite:underwrite@db:5432/underwrite

# --- API --------------------------------------------------------------------
API_PORT=8000
QUOTE_BASE_URL=http://localhost:8000
OPS_PASSWORD=changeme

# --- External services ------------------------------------------------------
# UW-020: https://console.anthropic.com
ANTHROPIC_API_KEY=
# UW-022: https://developer.company-information.service.gov.uk (free, self-serve)
COMPANIES_HOUSE_API_KEY=

# --- PDF rendering ----------------------------------------------------------
# 1 = render in-process with WeasyPrint (no AWS needed). See UW-055.
LOCAL_PDF=1
